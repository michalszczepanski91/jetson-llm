# Host-side deps for scripts/*.py and tests/ - these run directly (uv run),
# not inside the vLLM/llama.cpp containers (those are prebuilt vendor
# images, see docker-compose*.yml). Deliberately minimal: unlike
# jetson-vlm-lab (which needs opencv/numpy for image encoding), this repo is
# text-only, so stdlib (urllib, json) covers every HTTP call - only the
# config loader and test runner need real deps.
pytest==9.1.1
pyyaml==6.0.3
